# Migration `20201006122509-initial`

This migration has been generated by Erick Ruano at 10/6/2020, 12:25:09 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "LogAction" AS ENUM ('OPEN', 'DELETE', 'UPDATE', 'CREATE');

CREATE TYPE "UserType" AS ENUM ('OPERATIVO', 'ADMIN', 'SUPERVISOR');

CREATE TABLE "public"."Organization" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"description" text   ,
"id" text  NOT NULL ,
"logo" text   ,
"title" text  NOT NULL ,
"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Session" (
"access_token" text  NOT NULL ,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"device" text   ,
"device_info" text   ,
"id" text  NOT NULL ,
"id_token" text  NOT NULL ,
"ip" text   ,
"isAlive" boolean  NOT NULL ,
"refresh_token" text  NOT NULL ,
"refresh_token_expires" text   ,
"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"userId" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."User" (
"authId" text   ,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"email" text  NOT NULL ,
"id" text  NOT NULL ,
"name" text  NOT NULL ,
"organizationId" text   ,
"picture" text   ,
"type" "UserType" NOT NULL ,
"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."App" (
"body" jsonb   ,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"deletedAt" timestamp(3)   ,
"description" text   ,
"id" text  NOT NULL ,
"isArchived" boolean  NOT NULL DEFAULT false,
"isPublished" boolean  NOT NULL DEFAULT false,
"title" text  NOT NULL ,
"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"userId" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."AppLog" (
"action" "LogAction" NOT NULL ,
"appId" text  NOT NULL ,
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"id" text  NOT NULL ,
"record" jsonb  NOT NULL ,
"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"userId" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

ALTER TABLE "public"."Session" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."User" ADD FOREIGN KEY ("organizationId")REFERENCES "public"."Organization"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."App" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."AppLog" ADD FOREIGN KEY ("appId")REFERENCES "public"."App"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."AppLog" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201006122509-initial
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,91 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+model Organization {
+  id    String    @default(uuid()) @id
+  createdAt DateTime    @default(now()) 
+  updatedAt DateTime    @default(now()) 
+  title String
+  description String?
+  logo String?
+  user User[]
+}
+
+model Session {
+  access_token          String
+  createdAt             DateTime @default(now())
+  updatedAt DateTime    @default(now()) 
+  device                String?
+  device_info           String?
+  id                    String   @id @default(uuid())
+  id_token              String
+  ip                    String?
+  isAlive               Boolean
+  refresh_token         String
+  refresh_token_expires String?
+  userId                String
+  user                  User     @relation(fields: [userId], references: [id])
+}
+
+model User {
+    id    String    @default(uuid()) @id
+    createdAt DateTime    @default(now()) 
+    updatedAt DateTime    @default(now())
+    authId String?   
+    email String  @unique 
+    name String   
+    picture String?   
+    type UserType   
+    organizationId String?
+    organization  Organization?  @relation(fields: [organizationId], references: [id])
+    app App[]
+    session Session[]
+}
+
+model App {
+    id    String    @default(uuid()) @id
+    createdAt DateTime    @default(now()) 
+    updatedAt DateTime    @default(now())
+    deletedAt DateTime?
+    isArchived Boolean  @default(false)
+    title String   
+    description String?   
+    body Json?   
+    isPublished Boolean    @default(false) 
+    userId String?
+    user  User?  @relation(fields: [userId], references: [id])
+    app_log AppLog[]
+}
+
+model AppLog {
+    id    String    @default(uuid()) @id
+    createdAt DateTime    @default(now()) 
+    updatedAt DateTime    @default(now())
+    action LogAction   
+    record Json   
+    appId String
+    app  App  @relation(fields: [appId], references: [id])
+    userId String
+    user  User  @relation(fields: [userId], references: [id])
+}
+
+
+  enum LogAction {
+      OPEN
+      DELETE
+      UPDATE
+      CREATE
+  }
+  enum UserType {
+      OPERATIVO
+      ADMIN
+      SUPERVISOR
+  }
+
+
```


